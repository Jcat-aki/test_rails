<div>
  <%= f.label :assignee_team_member, '担当者（任意）', class: 'field-label' %>
  <%= f.select :assignee_team_member_id, options_from_collection_for_select(TeamMember.active.includes(:team).order(:name), 'id', ->(member) { "#{member.team.name} - #{member.name}" }, { selected: @task.assignee_team_member_id }), { include_blank: true }, class: 'field-input' %>
</div>
