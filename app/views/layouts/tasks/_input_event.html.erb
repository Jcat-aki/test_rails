<div>
  <%= f.label :event, 'イベント（任意）', class: 'field-label' %>
  <%= f.select :event_id, options_from_collection_for_select(Event.includes(:team).order(:starts_at), 'id', ->(event) { "#{event.team.name} - #{event.title}" }, { selected: @task.event_id }), { include_blank: true }, class: 'field-input' %>
</div>
